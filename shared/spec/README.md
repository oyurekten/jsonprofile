# jsonprofile Shared Specification

This directory contains language-neutral behavior notes for jsonprofile.

The initial contract is intentionally small: implementations must be able to
read the shared conformance manifest and distinguish valid profile documents
from invalid ones using the shared schema and fixtures.
